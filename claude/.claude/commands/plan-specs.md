Based on the codebase we are currently in, we want to generate a list of specs/\*.md that can be used to help inform what we need to build. Each spec should be a self-contained PRD. Read through specs/* to see what specs already exist.

Interview me relentlessly about every aspect of this project until we reach a shared understanding. Walk down each branch of the design tree, resolving dependencies between decisions one-by-one. For each question, provide your recommended answer.

Ask the questions one at a time.

If a question can be answered by exploring the codebase, explore the codebase instead.

Once we have reached a shared understanding, write our specs out.
