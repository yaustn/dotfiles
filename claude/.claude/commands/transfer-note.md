Read the note at the source path provided by the user. Clean it up and reformat it to match the formatting rules in the destination project's CLAUDE.md (if one exists), then write it to the destination path.

If the destination file already exists and has content, ask before overwriting.

Usage: /transfer-note <source> <destination>

Arguments: $ARGUMENTS
